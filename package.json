{
  "name": "cs2vault",
  "version": "3.3.0",
  "description": "CS2 Portfolio Tracker — Professional Investment Dashboard",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "build": "electron-builder --win --publish never",
    "release": "electron-builder --win --publish always"
  },
  "build": {
    "appId": "com.cs2vault.app",
    "productName": "CS2 Vault",
    "copyright": "CS2 Vault",
    "publish": [
      {
        "provider": "github",
        "owner": "Rudizzle123",
        "repo": "cs2vault-electron"
      }
    ],
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "assets/icon.ico",
      "signtoolOptions": {
        "//": "Signing is OFF until a certificate is provided. electron-builder auto-signs when CSC_LINK + CSC_KEY_PASSWORD are present in the environment (set as GitHub Actions secrets — see .github/workflows/build.yml and CODE-SIGNING.md). For an EV cert on a cloud HSM (e.g. Azure Trusted Signing / SSL.com eSigner), use a custom 'sign' hook instead; the file-based CSC_LINK path is OV-cert only.",
        "signingHashAlgorithms": ["sha256"]
      }
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false,
      "allowToChangeInstallationDirectory": false,
      "installerIcon": "assets/icon.ico",
      "uninstallerIcon": "assets/icon.ico",
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "CS2 Vault",
      "deleteAppDataOnUninstall": false
    },
    "files": [
      "src/**/*",
      "assets/**/*",
      "legal/**/*",
      "package.json"
    ],
    "compression": "maximum"
  },
  "dependencies": {
    "electron-store": "^8.2.0",
    "electron-updater": "^6.1.8"
  },
  "devDependencies": {
    "electron": "^29.1.0",
    "electron-builder": "^24.13.3"
  }
}